## unyt dashboard requirements

- The dashboard to have a filter by run_id
- Data about admin calls
  - This shows all the calls that start with `admin`
  - A cell that shows the average of all the admin calls
  - A cell that shows a graph of admin calls, call number and time taken
  - A cell that shows average of each call with visible numbers
  - A cell that shows max and min for each calls
- Data about zome calls

  - All the other calls
  - Ideally it should be similar look to the above part
  - A cell that shows the average of all the admin calls
  - A cell that shows a graph of admin calls, call number and time taken
  - A cell that shows average of each call with visible numbers
  - A cell that shows max and min for each calls

- Data about test run
  - Total number of zomeCalls that were made
  - length of the test run
  - Network init time
  - graph that shows diffrent agent keys on one side and the nuber of tx in the other and shows all the stats that we are logging like complete tx, actionable, parked spends, executed
